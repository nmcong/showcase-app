# ================================================
# VPS Deployment Configuration (Showcase-Only)
# ================================================
# Copy file này thành .env.deploy và điền thông tin của bạn
# KHÔNG commit .env.deploy lên git!

# ================================================
# VPS SSH Connection
# ================================================
VPS_HOST=your-vps-ip-or-domain
VPS_PORT=22
VPS_USER=root
VPS_PASSWORD=your-vps-password
# Hoặc sử dụng SSH key (để trống password nếu dùng key)
# VPS_SSH_KEY=~/.ssh/id_rsa

# ================================================
# Domain
# ================================================
APP_DOMAIN=showcase.yourdomain.com

# ================================================
# Application Configuration
# ================================================
APP_INSTALL_PATH=/var/www/showcase-app
APP_PORT=3000
NODE_ENV=production

# ================================================
# Git Repository
# ================================================
GIT_REPO_URL=https://github.com/yourusername/showcase-app.git
GIT_BRANCH=main

# ================================================
# SSL/TLS Configuration
# ================================================
SSL_EMAIL=your-email@example.com

# ================================================
# Performance Settings
# ================================================
# PM2 Settings (adjust based on your VPS specs)
PM2_INSTANCES=2           # Number of CPU cores
PM2_MAX_MEMORY=1024M      # Max memory per instance

# ================================================
# NOTE: Database and Keycloak have been removed
# This is a static showcase application only
# ================================================
